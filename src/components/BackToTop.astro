---
---
<script>
  let isVisible = false;
  const backToTopButton = document.getElementById('back-to-top');

  function toggleVisibility() {
    isVisible = window.pageYOffset > 300;
    console.log('Scroll position:', window.pageYOffset);
    console.log('Is visible:', isVisible);
    if (backToTopButton) {
      backToTopButton.style.display = isVisible ? 'block' : 'none';
      console.log('Button display:', backToTopButton.style.display);
    }
  }

  function scrollToTop() {
    console.log('Scrolling to top');
    window.scrollTo({
      top: 0,
      behavior: 'smooth'
    });
  }

  document.addEventListener('scroll', toggleVisibility);
  backToTopButton?.addEventListener('click', scrollToTop);
  console.log('BackToTop script initialized');
</script>
<button
  id="back-to-top"
  style="display: none; position: fixed; bottom: 20px; right: 20px; background-color: #333; color: white; padding: 10px; border-radius: 50%; border: none; cursor: pointer; box-shadow: 0 2px 5px rgba(0,0,0,0.3); z-index: 1000;"
>
  ↑
</button>
